2 api_name: tensorflow.raw_ops.LoadTPUEmbeddingFrequencyEstimatorParameters
3 api_description: Loads TPU embedding frequency estimator parameters into HBM after
  host configuration, typically to restore checkpointed training settings before execution.
4 api_signature: tf.raw_ops.LoadTPUEmbeddingFrequencyEstimatorParameters(parameters,num_shards,shard_id,table_id,table_name,config,name)
5 api_details: ''
6 api_parameters:
- parameters: Holds the float32 parameter values used by the frequency estimator optimization
    algorithm.
- num_shards: Specifies how many shards the embedding parameters are split across.
- shard_id: Identifies which shard this operation is loading parameters for.
- table_id: Optionally specifies the numeric ID of the embedding table to target,
    defaulting to -1.
- table_name: Optionally specifies the embedding table name to target, defaulting
    to an empty string.
- config: Optionally provides configuration data for the operation, defaulting to
    an empty string.
- name: Optionally assigns a name to the operation.
7 api_usage_example: null
8 api_shape: ''
