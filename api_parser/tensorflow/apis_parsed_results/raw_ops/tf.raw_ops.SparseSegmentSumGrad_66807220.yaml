2 api_name: tensorflow.raw_ops.SparseSegmentSumGrad
3 api_description: Computes the gradient of SparseSegmentSum by scattering the upstream
  gradient back to the original input positions, producing a tensor shaped like the
  input with the first dimension set to output_dim0.
4 api_signature: tf.raw_ops.SparseSegmentSumGrad(grad,indices,segment_ids)
5 api_details: ''
6 api_parameters:
- grad: The upstream gradient tensor to propagate back through the SparseSegmentSum
    operation.
- indices: The indices tensor originally passed to SparseSegmentSum, used to route
    gradients to the correct input positions.
- segment_ids: The segment IDs tensor originally passed to SparseSegmentSum, used
    to determine how gradients are grouped by segment.
7 api_usage_example: null
8 api_shape: ''
