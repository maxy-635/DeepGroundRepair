2 api_name: tensorflow.raw_ops.RaggedTensorToVariantGradient
3 api_description: Computes the gradient for `RaggedTensorToVariant` by reconstructing
  the dense-values gradient from variant-encoded ragged output gradients, using the
  outer row-splits and original dense-values shape.
4 api_signature: tf.raw_ops.RaggedTensorToVariantGradient(encoded_ragged_grad,row_splits,dense_values_shape)
5 api_details: ''
6 api_parameters:
- encoded_ragged_grad: Carries the encoded RaggedTensor gradient information in variant
    form for backpropagation.
- row_splits: Provides the outermost row-splits needed to reconstruct the original
    ragged structure.
- dense_values_shape: Specifies the original shape of the dense values used when the
    RaggedTensor was encoded.
7 api_usage_example: null
8 api_shape: ''
