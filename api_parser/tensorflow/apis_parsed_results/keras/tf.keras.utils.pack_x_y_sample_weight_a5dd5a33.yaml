2 api_name: tensorflow.keras.utils.pack_x_y_sample_weight
3 api_description: Packs user-provided x, y, and sample_weight values into the tuple
  format expected by Model.fit().
4 api_signature: tf.keras.utils.pack_x_y_sample_weight(x,y,sample_weight)
5 api_details: ''
6 api_parameters:
- x: Input features passed to the model.
- y: Ground-truth target values passed to the model.
- sample_weight: Weights applied to individual samples or elements.
7 api_usage_example:
- x = ops.ones((10, 1))
- data = pack_x_y_sample_weight(x)
- isinstance(data, ops.Tensor)
- y = ops.ones((10, 1))
- data = pack_x_y_sample_weight(x, y)
- isinstance(data, tuple)
- x, y = data
8 api_shape: ''
