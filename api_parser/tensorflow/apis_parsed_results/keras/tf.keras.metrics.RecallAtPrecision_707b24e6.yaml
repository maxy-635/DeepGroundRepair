2 api_name: tensorflow.keras.metrics.RecallAtPrecision
3 api_description: Computes the maximum recall achievable while keeping precision
  at or above a specified target, using accumulated TP/TN/FP/FN counts and optional
  sample weights or class filtering.
4 api_signature: tf.keras.metrics.RecallAtPrecision(precision,num_thresholds)
5 api_details: ''
6 api_parameters:
- precision: Sets the target precision level that RecallAtPrecision tries to achieve.
- num_thresholds: Controls how many thresholds are evaluated when searching for the
    threshold that matches the target precision.
7 api_usage_example:
- m.reset_state()
- m.update_state([0, 0, 1, 1], [0, 0.5, 0.3, 0.9],
- '               sample_weight=[1, 0, 0, 1])'
- m.result()
8 api_shape: ''
