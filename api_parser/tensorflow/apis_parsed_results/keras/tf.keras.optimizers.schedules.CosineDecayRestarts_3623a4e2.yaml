2 api_name: tensorflow.keras.optimizers.schedules.CosineDecayRestarts
3 api_description: A learning-rate schedule that cosine-decays the rate from the initial
  value to alpha over a cycle, then restarts periodically with each cycle optionally
  growing longer by t_mul and scaling the restart peak by m_mul.
4 api_signature: tf.keras.optimizers.schedules.CosineDecayRestarts(initial_learning_rate,first_decay_steps)
5 api_details: ''
6 api_parameters:
- initial_learning_rate: Sets the starting learning rate for the schedule.
- first_decay_steps: Specifies how many steps each cosine decay cycle lasts before
    restarting.
7 api_usage_example: null
8 api_shape: ''
