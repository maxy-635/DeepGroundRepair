2 api_name: tensorflow.keras.utils.unpack_x_y_sample_weight
3 api_description: Unpacks input data tuples into x, y, and optional sample_weight
  for use in custom train, test, or predict steps.
4 api_signature: tf.keras.utils.unpack_x_y_sample_weight(data)
5 api_details: ''
6 api_parameters:
- data: A tuple containing the input data, and optionally the target labels and sample
    weights, to be unpacked into x, y, and sample_weight.
7 api_usage_example:
- features_batch = ops.ones((10, 5))
- labels_batch = ops.zeros((10, 5))
- data = (features_batch, labels_batch)
- '# `y` and `sample_weight` will default to `None` if not provided.'
- x, y, sample_weight = unpack_x_y_sample_weight(data)
- sample_weight is None
8 api_shape: ''
