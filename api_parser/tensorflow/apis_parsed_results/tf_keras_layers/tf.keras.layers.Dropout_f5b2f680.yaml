2 api_name: tensorflow.keras.layers.Dropout
3 api_description: The Dropout layer randomly sets a fraction of input units to 0
  during training to prevent overfitting, scaling remaining inputs up to maintain
  the sum. It only applies during training, with no effect during inference.
4 api_signature: tf.keras.layers.Dropout(rate)
5 api_details: ''
6 api_parameters:
- rate: A float between 0 and 1 that specifies the fraction of input units to drop
    during training to prevent overfitting.
7 api_usage_example: null
8 api_shape: null
