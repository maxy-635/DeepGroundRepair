2 api_name: tensorflow.keras.layers.Softmax
3 api_description: The Softmax layer converts logits (raw prediction scores) into
  probabilities by exponentiating the inputs and normalizing them, ensuring that the
  output values sum to one.
4 api_signature: tf.keras.layers.Softmax(axis)
5 api_details: ''
6 api_parameters:
- axis: Defines the dimension or dimensions along which the softmax function is computed,
    allowing for flexibility in how the normalization is applied across multi-dimensional
    data.
7 api_usage_example:
- oftmax_layer = keras.layers.activations.Softmax()
- nput = np.array([1.0, 2.0, 1.0])
- esult = softmax_layer(input)
8 api_shape: null
