2 api_name: tensorflow.keras.layers.Cropping1D
3 api_description: Cropping1D is a Keras layer that removes specified numbers of values
  from the beginning and/or end of 1D input sequences, effectively cropping along
  the time dimension.
4 api_signature: tf.keras.layers.Cropping1D(cropping)
5 api_details: ''
6 api_parameters:
- cropping: Specifies the number of values to crop from the beginning and end of the
    input sequence, either as a single integer, a tuple of two integers, or a dictionary.
7 api_usage_example:
- input_shape = (2, 3, 2)
- x = np.arange(np.prod(input_shape)).reshape(input_shape)
- x
- y = keras.layers.Cropping1D(cropping=1)(x)
- y
8 api_shape: null
