2 api_name: tensorflow.data.experimental.DatasetInitializer
3 api_description: Creates a table initializer from a `tf.data.Dataset`, ensuring
  the dataset meets the required specifications and raising `ValueError` if it does
  not.
4 api_signature: tf.data.experimental.DatasetInitializer(dataset)
5 api_details: ''
6 api_parameters:
- dataset: The tf.data.Dataset supplying key-value scalar tuples, where the first
    scalar is used as the key and the second as the value.
7 api_usage_example:
- keys = tf.data.Dataset.range(100)
- values = tf.data.Dataset.range(100).map(
- '    lambda x: tf.strings.as_string(x * 2))'
- ds = tf.data.Dataset.zip((keys, values))
- init = tf.data.experimental.DatasetInitializer(ds)
- table = tf.lookup.StaticHashTable(init, "")
- table.lookup(tf.constant([0, 1, 2], dtype=tf.int64)).numpy()
8 api_shape: ''
