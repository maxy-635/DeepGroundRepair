2 api_name: paddle.optimizer.lr.NaturalExpDecay
3 api_description: Applies natural exponential decay to the initial learning rate
  over time.
4 api_signature: paddle.optimizer.lr.NaturalExpDecay(learning_rate,gamma)
5 api_details: ''
6 api_parameters:
- learning_rate: Sets the starting learning rate as a Python float.
- gamma: Controls the exponential decay rate of the learning rate and must be greater
    than 0.0.
7 api_usage_example:
- import paddle
- value = paddle.arange(26, dtype='float32')
- a = paddle.reshape(value, [2, 13])
- linear = paddle.nn.Linear(13, 5)
- adadelta = paddle.optimizer.Adadelta(learning_rate=0.0003, epsilon=1e-06, rho=0.95,
- '                            parameters = linear.parameters())'
- out = linear(a)
- out.backward()
- adadelta.step()
- adadelta.clear_grad()
8 api_shape: {}
