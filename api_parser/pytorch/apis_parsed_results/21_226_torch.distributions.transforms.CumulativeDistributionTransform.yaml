2 api_name: torch.distributions.transforms.CumulativeDistributionTransform
3 api_description: Applies a distribution’s cumulative distribution function to map
  values into [0, 1], transforming samples to their corresponding cumulative probabilities.
4 api_signature: CumulativeDistributionTransform(distribution)
5 api_details: ''
6 api_parameters:
- distribution: The distribution whose cumulative distribution function defines the
    transform.
7 api_usage_example:
- '# Construct a Gaussian copula from a multivariate normal.'
- base_dist = MultivariateNormal(
- '    loc=torch.zeros(2),'
- '    scale_tril=LKJCholesky(2).sample(),'
- )
- transform = CumulativeDistributionTransform(Normal(0, 1))
- copula = TransformedDistribution(base_dist, [transform])
8 api_shape: null
