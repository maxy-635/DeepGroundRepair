2 api_name: torch.nn.functional.scaled_dot_product_attention
3 api_description: The torch.nn.functional.scaled_dot_product_attention function computes
  the scaled dot-product attention, optionally applying a causal mask, scaling factor,
  and group query attention (GQA) for efficient attention mechanism in neural networks.
4 api_signature: torch.nn.functional.scaled_dot_product_attention(query,key,value)
5 api_details: ''
6 api_parameters:
- query: The query tensor, which is used to match against keys, with shape (N, ...,
    Hq, L, E).
- key: The key tensor, which is compared against the query, with shape (N, ..., H,
    S, E).
- value: The value tensor, which provides the output data, with shape (N, ..., H,
    S, Ev).
- scale: A scaling factor applied to the dot product before softmax, defaulting to
    1/sqrt(E) if None.
7 api_usage_example:
- '# Efficient implementation equivalent to the following:'
- def scaled_dot_product_attention(query, key, value, attn_mask=None, dropout_p=0.0,
- '        is_causal=False, scale=None, enable_gqa=False) -> torch.Tensor:'
- '    L, S = query.size(-2), key.size(-2)'
- '    scale_factor = 1 / math.sqrt(query.size(-1)) if scale is None else scale'
- '    attn_bias = torch.zeros(L, S, dtype=query.dtype, device=query.device)'
- '    if is_causal:'
- '        assert attn_mask is None'
- '        temp_mask = torch.ones(L, S, dtype=torch.bool).tril(diagonal=0)'
- '        attn_bias.masked_fill_(temp_mask.logical_not(), float("-inf"))'
- '    if attn_mask is not None:'
- '        if attn_mask.dtype == torch.bool:'
- '            attn_bias.masked_fill_(attn_mask.logical_not(), float("-inf"))'
- '        else:'
- '            attn_bias = attn_mask + attn_bias'
- '    if enable_gqa:'
- '        key = key.repeat_interleave(query.size(-3)//key.size(-3), -3)'
- '        value = value.repeat_interleave(query.size(-3)//value.size(-3), -3)'
- '    attn_weight = query @ key.transpose(-2, -1) * scale_factor'
- '    attn_weight += attn_bias'
- '    attn_weight = torch.softmax(attn_weight, dim=-1)'
- '    attn_weight = torch.dropout(attn_weight, dropout_p, train=True)'
- '    return attn_weight @ value'
8 api_shape: null
