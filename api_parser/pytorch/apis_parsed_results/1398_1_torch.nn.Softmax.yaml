2 api_name: torch.nn.Softmax
3 api_description: Applies the Softmax function to an n-dimensional input Tensor,
  normalizing the input values to a probability distribution across specified dimensions.
4 api_signature: torch.nn.Softmax(dim)
5 api_details: ''
6 api_parameters:
- dim: Specifies the dimension of the input tensor along which the Softmax function
    will be applied.
7 api_usage_example:
- ' m = nn.Softmax(dim=1)'
- ' input = torch.randn(2, 3)'
- ' output = m(input)'
8 api_shape:
  input tensor shape: (*) where * means, any number of additional dimensions
  output tensor shape: (*), same shape as the input
  tensor shape transform formulas: {}
