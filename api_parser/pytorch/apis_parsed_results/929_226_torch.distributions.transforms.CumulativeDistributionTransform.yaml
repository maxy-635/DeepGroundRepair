2 api_name: torch.distributions.transforms.CumulativeDistributionTransform
3 api_description: Maps inputs through a distribution’s cumulative distribution function,
  transforming values into cumulative probability space.
4 api_signature: torch.distributions.transforms.CumulativeDistributionTransform(distribution)
5 api_details: ''
6 api_parameters:
- distribution: Specifies the distribution whose cumulative distribution function
    is used to transform values.
7 api_usage_example:
- '# Construct a Gaussian copula from a multivariate normal.'
- base_dist = MultivariateNormal(
- '    loc=torch.zeros(2),'
- '    scale_tril=LKJCholesky(2).sample(),'
- )
- transform = CumulativeDistributionTransform(Normal(0, 1))
- copula = TransformedDistribution(base_dist, [transform])
8 api_shape: null
