2 api_name: torch.nn.ParameterList
3 api_description: torch.nn.ParameterList is a container that holds a list of parameters,
  allowing for easy management and iteration over a collection of learnable weights
  in a neural network.
4 api_signature: torch.nn.ParameterList(values)
5 api_details: ''
6 api_parameters: []
7 api_usage_example:
- 'class MyModule(nn.Module):'
- '    def __init__(self) -> None:'
- '        super().__init__()'
- '        self.params = nn.ParameterList([nn.Parameter(torch.randn(10, 10)) for i
  in range(10)])'
- '    def forward(self, x):'
- '        # ParameterList can act as an iterable, or be indexed using ints'
- '        for i, p in enumerate(self.params):'
- '            x = self.params[i // 2].mm(x) + p.mm(x)'
- '        return x'
8 api_shape: null
