2 api_name: torch.distributed.autograd.context
3 api_description: Creates and manages a distributed autograd context that uniquely
  identifies a forward/backward pass across workers so each worker can store and use
  the metadata needed to run distributed backpropagation correctly.
4 api_signature: class torch.distributed.autograd.context
5 api_details: ''
6 api_parameters: []
7 api_usage_example:
- ' import torch.distributed.autograd as dist_autograd'
- ' with dist_autograd.context() as context_id:'
- '     t1 = torch.rand((3, 3), requires_grad=True)'
- '     t2 = torch.rand((3, 3), requires_grad=True)'
- '     loss = rpc.rpc_sync("worker1", torch.add, args=(t1, t2)).sum()'
- '     dist_autograd.backward(context_id, [loss])'
8 api_shape: null
