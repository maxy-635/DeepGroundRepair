2 api_name: torch.nn.ReplicationPad2d
3 api_description: Replicates the boundary values of the input tensor to pad it on
  all sides.
4 api_signature: torch.nn.ReplicationPad2d(padding)
5 api_details: ''
6 api_parameters:
- padding: Specifies the size of the padding; if an integer, applies the same padding
    to all boundaries, and if a 4-tuple, applies different padding to each side.
7 api_usage_example:
- ' m = nn.ReplicationPad2d(2)'
- ' input = torch.arange(9, dtype=torch.float).reshape(1, 1, 3, 3)'
- ' input'
- ' m(input)'
- ' # using different paddings for different sides'
- ' m = nn.ReplicationPad2d((1, 1, 2, 0))'
- ' m(input)'
8 api_shape:
  input tensor shape: (N, Channel_in, Height_in, Width_in)
  output tensor shape: (N, Channel_out, Height_out, Width_out)
  tensor shape transform formulas: {}
